/** Placeholder: real x402 challenge parsing hooks into @x402/core in a follow-up. */
export type Parsed402 = { raw: string };

export function parse402Body(_body: unknown): Parsed402 {
  return { raw: '' };
}
