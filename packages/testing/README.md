# @leash/testing

In-memory x402 mock server, local facilitator, fixtures, and **`leash-conformance`** CLI for JSONL receipt feeds (uses `@leash/core` `verifyReceiptChain`).

```bash
pnpm --filter @leash/testing build
pnpm exec leash-conformance path/to/receipts.jsonl
```
