# @leashmarket/testing

In-memory x402 mock server, local facilitator, fixtures, and **`leash-conformance`** CLI for JSONL receipt feeds (uses `@leashmarket/core` `verifyReceiptChain`).

```bash
pnpm --filter @leashmarket/testing build
pnpm exec leash-conformance path/to/receipts.jsonl
```
