# @leash/buyer-kit

**`createBuyer`** — policy from `@leash/schemas`, outbound calls via `@leash/core` `x402Fetch`, receipts via `finalizeReceipt`.

```ts
import { createBuyer } from '@leash/buyer-kit';
```
