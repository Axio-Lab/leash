export default function SpendSettingsPage() {
  return (
    <div className="space-y-4 text-sm text-fg-muted max-w-xl">
      <p>
        Spend controls are currently configured during onboarding defaults and enforced during
        payment calls. A dedicated spend-control UI can be added here once treasury management is
        brought directly into the chat-first surface.
      </p>
    </div>
  );
}
