{
  "name": "@leash/facilitator-app",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "leash-facilitator",
    "dev": "tsx ./src/dev.ts",
    "build": "echo 'no build — uses @leash/facilitator binary'",
    "lint": "echo lint:facilitator-app",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.7",
    "@leash/facilitator": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
