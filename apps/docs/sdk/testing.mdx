---
title: '@leashmarket/testing'
description: Fixtures, in-memory facilitator, and the leash-conformance CLI.
---

```bash
pnpm --filter @leashmarket/testing build
pnpm exec leash-conformance path/to/receipts.jsonl
```

The CLI returns exit `0` and a JSON `{ ok: true, count }` when the feed parses + chain-verifies (using `@leashmarket/core` `verifyReceiptChain`), else exit `1` with a `{ ok: false, line, reason }` payload.
